import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#000000',
        secondary: '#f1c40f',
        tertiary: '#a1b2c3',
        headers: '#ffffff',
        button: '#af8e79',
    },
};